# H3531 RetroArch Stage 2.2 - statically linked NES/Famicom core
#
# Keeps the physically proven Stage-1 HIFB + evdev + RGUI path unchanged.
# Stage-2.2 aligns the menu with a single statically linked FCEUmm core:
# - no dynamic Load Core browser,
# - exact RetroArch filter key spelling,
# - official FCEUmm core-info metadata packaged beside the frontend.
# H3531 AO audio comes later.

video_driver = "h3531"
video_fullscreen = "true"
video_windowed_fullscreen = "false"
video_vsync = "false"
video_threaded = "false"
video_refresh_rate = "60.000000"

input_driver = "h3531evdev"
input_joypad_driver = "null"

# Stage 2.2 still excludes H3531 AO so core/video/input can be isolated first.
audio_driver = "null"
audio_enable = "false"

menu_driver = "rgui"
menu_pause_libretro = "true"
menu_swap_ok_cancel_buttons = "false"
rgui_browser_directory = "/mnt/usb/games/nes"

# This build has one statically linked core, so the normal dynamic-core browser
# is misleading and is intentionally hidden.
menu_show_load_core = "false"
menu_show_load_content = "true"

# RetroArch defaults "Filter Unknown Extensions" to ON. Disable it for the
# first physical content-loading tests. Note the historical capital C in the
# second key: this is the exact config spelling used by current RetroArch.
menu_navigation_browser_filter_supported_extensions_enable = "false"
filter_by_current_Core = "false"

# Keyboard -> RetroPad mapping for first physical NES test.
# Arrows: D-pad, Enter: A, Backspace: B, Space: Start, Right Shift: Select.
input_player1_up = "up"
input_player1_down = "down"
input_player1_left = "left"
input_player1_right = "right"
input_player1_a = "enter"
input_player1_b = "backspace"
input_player1_start = "space"
input_player1_select = "rshift"

# F1 opens/closes RetroArch menu. F12 exits cleanly to the H3531 supervisor.
input_exit_emulator = "f12"
input_menu_toggle = "f1"

savefile_directory = "/mnt/usb/H3531/USER/retroarch/saves"
savestate_directory = "/mnt/usb/H3531/USER/retroarch/states"
playlist_directory = "/mnt/usb/H3531/USER/retroarch/playlists"
system_directory = "/mnt/usb/H3531/USER/retroarch/system"

# No dynamic .so core is expected in this stage. The info directory is still
# useful because RetroArch menu/core-info logic consumes FCEUmm metadata there.
libretro_directory = "/mnt/usb/H3531/APPS/retroarch/cores"
core_info_directory = "/mnt/usb/H3531/APPS/retroarch/info"

config_save_on_exit = "false"
log_verbosity = "true"

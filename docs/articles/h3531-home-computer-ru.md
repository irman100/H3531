# H3531 Home Computer: как старый видеорегистратор стал экспериментальным домашним компьютером

Попались мне в руки несколько морально устаревших видеорегистраторов. После разборки стало понятно, что внутри находится не просто специализированное DVR-устройство, а полноценная вычислительная платформа.

Плата AHB70XXT16-3531 V2.01 построена вокруг HiSilicon Hi3531. Linux видит два ARMv7-ядра, на плате установлена память, USB, Ethernet, SATA, HDMI, VGA, UART и SPI Flash. По сути это небольшой Linux-компьютер, просто изначально предназначенный для видеонаблюдения.

Мне стало интересно: можно ли перестать относиться к нему как к регистратору и сделать из него самостоятельную экспериментальную компьютерную среду?

Я не являюсь профессиональным разработчиком embedded-систем. Но сегодня у одного человека есть инструмент, которого раньше не было, - искусственный интеллект. Он не заменяет реальные эксперименты: плату все равно нужно подключать, смотреть UART, проверять изображение, ловить ошибки и снова собирать программы. Но он позволяет быстрее разбираться в загрузчике, Linux, C, framebuffer, SDL и чужом исходном коде. Так появился проект **H3531 Home Computer**.

## С чего начался путь

Сначала нужно было понять заводскую систему: U-Boot, Linux 3.0.8, SPI Flash, модули HiSilicon и штатное DVR-приложение Sofia. Серия reverse-engineering экспериментов позволила восстановить цепочку видеовывода и доказать, что для собственной картинки на HDMI заводская Sofia не нужна.

Удалось самостоятельно поднять цепочку:

```text
MPP -> VO -> HDMI -> HIFB -> /dev/fb0
```

После этого программа пользователя смогла получить framebuffer 1280x720 и вывести собственную картинку. Именно этот момент превратил проект из попытки "поковырять регистратор" в идею собственного домашнего компьютера.

## Безопасная загрузка

Главный принцип проекта - не прошивать экспериментальные образы в SPI Flash и не использовать `saveenv`. Заводское ядро загружается через U-Boot, а наша система стартует с USB как внешний CramFS RAMDisk. После перезагрузки постоянная память остается заводской.

Такой подход позволил спокойно делать много неудачных экспериментов: если что-то ломалось, устройство не превращалось в кирпич.

## Monitor, FILES и пользовательская среда

Следующим этапом стал **H3531 Monitor** - собственная оболочка поверх framebuffer. Сначала это был простой терминал с несколькими командами. Затем появились PTY, shell, обработка USB-клавиатуры и мыши, история команд, функциональные клавиши и файловый менеджер **FILES**.

Проект постепенно разделился на несколько частей:

```text
U-Boot
  -> Linux 3.0.8
    -> H3531.IMG / CramFS
      -> h3531-video-init
      -> h3531-input-init
      -> USB / hotplug
      -> session supervisor
      -> h3531-monitor
        -> FILES
          -> BASIC / APP / emulator experiments
```

Важное архитектурное решение: графическое приложение не работает параллельно с Monitor. При запуске приложения Monitor заменяется через `execve()`, программа получает экран и ввод целиком, а после выхода supervisor запускает Monitor снова.

## BASIC и первые приложения

Чтобы система ощущалась не просто как загрузочный эксперимент, а как компьютер, были добавлены BASIC и графические программы. Запускались Tiny BASIC, Bywater BASIC и Matrix Brandy BASIC VI. На реальной плате проверены BASIC-программы и графический вывод через наш SDL 1.2 backend.

Для этого был сделан H3531 SDL backend: программа рисует в обычную логическую поверхность, а backend преобразует ее в физический framebuffer Hi3531. Позднее кэширование таблиц масштабирования заметно ускорило вывод.

## Эмуляторы как один из классов приложений

Когда заработали обычные программы, захотелось проверить еще один класс нагрузки - эмуляторы. Первым экспериментом стал FBZX 3.1.0, эмулятор ZX Spectrum.

Важно: Spectrum не является конечной целью проекта. Это не попытка сделать "новый ZX Spectrum". Это только один наглядный тест того, что старый DVR способен запускать сложное стороннее ПО через нашу оболочку.

Сегодня подтвержден сценарий:

```text
FILES -> файл .Z80 -> FBZX -> запущенная игра/программа
```

`.Z80`-файлы открываются прямо из файлового менеджера. Поддержка других форматов, автоматическая загрузка кассетных образов, звук, более удобная PC-раскладка клавиатуры и другие эмуляторы - это будущие направления.

## Что получилось

Мы начали со старого DVR, затем разобрались с его загрузкой и видеоподсистемой, получили собственный HDMI-вывод, сделали безопасную RAM-загрузку, написали Monitor и FILES, подключили клавиатуру и USB, запустили BASIC, создали SDL-слой и начали запускать сторонние программы.

Основная цель **H3531 Home Computer** - показать, что забытое специализированное устройство можно превратить в открытую экспериментальную компьютерную платформу. Spectrum здесь - только один пример возможностей, а не финальная точка проекта.
